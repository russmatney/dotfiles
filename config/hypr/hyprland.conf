# https://wiki.hyprland.org/Configuring/
#
# THE HYPRLAND HOME PAGE
#
#

#########################
## DISPLAY
# Machine-specific monitor configuration (symlinked by setup_machine_config.bb)
source = monitors-machine.conf
# source = monitors.conf

#########################
## ENV VARS
source = env_vars.conf

#########################
## SCRIPTS/EXECS
source = programs.conf
source = autostart.conf

#########################
## NONSENSE
source = permissions.conf
source = input.conf

#########################
## PRETTY
source = look_and_feel.conf

#########################
## BINDINGS
source = bindings.conf
source = toggles.conf

#########################
## MACHINE-SPECIFIC OVERRIDES
# Machine-specific overrides (symlinked by setup_machine_config.bb)
source = overrides-machine.conf

#########################
## CLIENTS AND WORKSPACES
source = clients.conf
source = workspaces.conf
