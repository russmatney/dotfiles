# Machine-specific overrides for yoshi

# Yoshi-specific keybindings
# bind = $mainMod, F1, exec, some-yoshi-specific-command

# Yoshi-specific window rules
# windowrule = float, class:yoshi-specific-app

# Yoshi-specific environment variables
env = YOSHI,1

# Yoshi-specific performance settings
decoration {
  blur {
    enabled = false
  }
}
